{
  "name": "school-climate-hub-chat-worker",
  "version": "0.1.0",
  "description": "Cloudflare Worker proxying chat queries to Anthropic Claude with the School Climate Hub dataset as system context.",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "set-secret": "wrangler secret put ANTHROPIC_API_KEY"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251001.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.86.0"
  }
}
