{
  "name": "school-climate-hub-e2e",
  "version": "0.1.0",
  "private": true,
  "description": "Playwright smoke tests for the School Climate Hub operator console (static site).",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
